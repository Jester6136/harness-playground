- name: smoke_hello
  input: "Say hello in one sentence."
  expected:
    contains: "hello"

- name: smoke_list_dir
  input: "List the files in the current directory."
  expected:
    tool_called: ls

- name: smoke_read_file
  input: "Read the file harness/config.py and tell me what TEMPERATURE is set to."
  expected:
    tool_called: read_file

- name: smoke_math
  input: "What is 17 * 23? Answer with just the number."
  expected:
    contains: "391"

- name: smoke_skill_dispatch
  input: "Use the summarize_codebase skill to briefly describe this project."
  expected:
    contains: "harness"
